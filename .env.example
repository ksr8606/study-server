# 이 파일을 .env 로 복사해서 쓰면 됨:  cp .env.example .env
# (.env 는 .gitignore 로 제외됨 — 실제 비밀은 Git에 올리지 않음)

DATABASE_URL=postgresql+psycopg://postgres:studypass@localhost:5432/studydb

# JWT 서명용 비밀키 — 아래 명령으로 생성해서 채워: openssl rand -hex 32
SECRET_KEY=여기에_랜덤_문자열_넣기
